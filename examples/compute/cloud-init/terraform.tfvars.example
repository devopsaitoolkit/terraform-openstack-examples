# Copy to terraform.tfvars and adjust for your cloud.
# (terraform.tfvars is gitignored so you never commit environment specifics.)

cloud         = "openstack"
instance_name = "app-cloudinit-01"
flavor_name   = "m1.small"
image_name    = "ubuntu-22.04"
network_name  = "private"
key_pair_name = "my-keypair"

# Paste the contents of your public key (e.g. ~/.ssh/id_ed25519.pub).
ssh_authorized_key = "ssh-ed25519 AAAAC3NzaC1lZDI1NTE5AAAA... user@example"

packages = ["htop", "curl", "jq"]

security_group_names = ["default"]
tags                 = ["managed-by:terraform", "env:dev", "app:api"]
